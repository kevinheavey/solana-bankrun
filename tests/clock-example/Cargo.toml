cargo-features = ["workspace-inheritance"]

[package]
name = "bankrun-clock-example"
version = "0.0.0"
edition = { workspace = true }
include = ["/src"]

[dependencies]
solana-program = { workspace = true }

[lib]
crate-type = ["cdylib"]
